export namespace Const {
    export enum table_style_class {
        p_datatable_sm = "p-datatable-sm",
    }

    export enum severity_msg {
        success = "success",
        info = "info",
        warn = "warn",
        error = "error",
    }
}
